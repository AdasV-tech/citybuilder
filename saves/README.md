# saves/

This folder is reserved for a future "export/import save file" feature.

The current version stores saves in the browser's Local Storage (see
`js/save/SaveManager.js`), so nothing is written here yet — it exists so the
save system has an obvious place to grow into (e.g. downloadable `.json`
save files) without restructuring the project.
